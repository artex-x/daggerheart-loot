# Generated data, not authored: node tools/build.js writes these, and
# reformatting a machine-written file produces an unreadable diff for no
# behaviour change.
data.js
data.json
catalog.csv
i/
img/
og/
card/

# Permanent, decided at phase 8 (this was left open at R0c, issue 47, for
# app/index.html, as "not the batch that decides its formatting" - it is
# decided now; 404.html, added later in phase 8 (B4), follows the same
# decision on the same reasoning, not a copy-pasted rationale): both are
# hand-authored, flat, no indentation, HTML5-style void elements with no
# trailing slash. Prettier's HTML formatter would indent every line and add
# a self-closing `/>` to every `<meta>`/`<link>` - a ~60-line diff for no
# behaviour change, same reasoning as the generated files above even though
# neither of these two is generated.
app/index.html
404.html
dist/
coverage/
node_modules/
docs/fixtures/

# Hook runtime state: gitignored (.claude/.gitignore), but Prettier does not
# read a nested .gitignore, so format:check would otherwise trip on whatever
# a live hook wrote during the session.
.claude/.check-cache.json
.claude/.check-index
.claude/.hook-state.json

# A dispatched agent's isolated worktree is host infrastructure, not source,
# and can land nested under this tree's own .claude/ directory unpredictably
# (a host placement choice, not this repository's). A repo-root
# `prettier --check .` walks into one if it is there, reddening format:check
# (hence npm run check) for a reason no diff in this tree explains - it
# blocked R0c C3's commit gate once already (issues/47/handoff.md, "R0c C3").
# Also in .gitignore, which is the superset: Prettier's own default
# `--ignore-path` is [".gitignore", ".prettierignore"] together, but
# tree-key.mjs's commit-gate fingerprint runs `git add -A`, which reads only
# .gitignore, so this file alone would silence Prettier while leaving the
# path inside every fingerprint.
.claude/worktrees/

# Markdown is wrapped by hand: in the READMEs and specs a line break carries
# meaning, and the licence notice has to stay on one line (tests/derived.js pins it).
*.md
