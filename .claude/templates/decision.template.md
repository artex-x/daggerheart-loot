# <YYYY-MM-DD> - <title: the decision in one sentence, 80 characters or less; it is the citation key and never changes>

<!-- Record a decision only when an alternative was rejected. Behaviour
goes to docs/specs/; hooks, tooling and host facts to .claude/README.md;
a defect kept on purpose to docs/specs/DEBT.md. File:
docs/decisions/<YYYY-MM-DD>-<slug>.md; then run `node tools/decisions.js`
to rebuild the index docs/DECISIONS.md. Body: at most fifteen non-blank
lines. Keep the line shapes below; a status line, when present, comes
first; other bullets (Evidence, Accepted trade-off) go after Rejected.
Pointer shapes: `- Superseded by "<title>" (<date>).`, `- Superseded in
part by "<title>" (<date>): <what>.`, `- Amended by "<title>" (<date>):
<what>.` (written into the older file by the batch that replaces it) and
their mirrors `- Supersedes "<title>" (<date>).`, `- Supersedes in part
"<title>" (<date>): <what>.`, `- Amends "<title>" (<date>): <what>.` in
the newer file. Both directions are required. -->

- Task: `<id>` (<who decided, when>).
- Decision: <what holds now, and where it lives>.
- Rejected: <alternative> (<why>); <alternative> (<why>).
- Supersedes "<title>" (<YYYY-MM-DD>).
